#include <stdio.h>
#include <stdlib.h>
int main()
{
    int a, b, sum;
    a = 1;
    b = 2;
    sum = a + b;
    if (a!=b)
    {
        if (sum==2)
        {
            printf("2");
        }
    }
}